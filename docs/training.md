# Training Guide

Use `mimicplay train --config configs/train_bc.yaml` to train a behavioral cloning policy once you have collected demonstrations.

Configuration files control hyperparameters such as batch size, learning rate, and augmentation.

