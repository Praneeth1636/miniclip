# Architecture

MimicPlay is organized into:

- `envs/` for custom Pygame environments.
- `data/` for HDF5 recording and datasets.
- `models/` for BC and VLA policies.
- `training/` for training loops.
- `evaluation/` for metrics and GradCAM.
- `dashboard/` for the Streamlit interface.

